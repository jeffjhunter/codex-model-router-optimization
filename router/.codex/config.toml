# CMRO is an explicit Sol -> Luna/Terra -> Sol routing profile.
# These root settings apply to every Codex task in the target repository.
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"

[agents]
max_threads = 4
max_depth = 1
interrupt_message = true
