interface:
  display_name: "Route Codex Work"
  short_description: "Run verified Sol, Luna/Terra, Sol routing"
  default_prompt: "Use $route-codex-work to plan this request, run a model-pinned Luna or Terra task, and obtain independent Sol review with runtime evidence."

policy:
  allow_implicit_invocation: false
